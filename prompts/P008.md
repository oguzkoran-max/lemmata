## P008 — Test suite
- **Date:** 2026-03-28
- **Category:** testing
- **Prompt:** Write tests in tests/ directory. conftest with fixtures, test_config, test_preprocessing, test_modelling (including CRITICAL determinism test), test_file_io, test_visualisation, test_smoke (integration). Use pytest, mock spaCy for unit tests, real model for smoke.
- **Response summary:** Created 8 test files with ~70 test cases across 6 modules. conftest.py: shared fixtures (sample texts, corpus, topics, doc_topic_matrix, mock_nlp with MockToken/MockSent/MockDoc). test_config: 20 tests covering all constants, types, POS_PRESETS, get_df_auto boundaries. test_preprocessing: 16 tests covering detect_encoding, clean_text (BOM, null, control, hyphen, spaces, CRLF, NFC), chunk_text (labels, empty), process_documents (trace keys, stopwords, chunking). test_modelling: 15 tests covering build_dtm, run_lda, DETERMINISM (same seed → identical), topic ordering, compute_coherence, sweep_coherence NotImplementedError. test_file_io: 16 tests covering read_file, text_from_paste, get_file_preview, export_zip (6 files verified), get_environment_info. test_visualisation: 16 tests covering all chart functions, colour consistency, coherence display bands. test_smoke: 3 @pytest.mark.slow integration tests (full pipeline, determinism, single-file chunking) using real en_core_web_sm.
- **Decision:** Mock spaCy in unit tests via conftest _MockToken/_MockDoc/_MockSent to avoid model downloads. Smoke tests marked @pytest.mark.slow for CI separation. Determinism test runs LDA twice with seed=42 and asserts np.array_equal on doc_topic matrices. Added [tool.pytest.ini_options] and [project.optional-dependencies] test to pyproject.toml.
- **Validation:** Files created, structure verified. Tests not yet executed (dependencies not installed).
- **Files changed:** tests/__init__.py (new), tests/conftest.py (new), tests/test_config.py (new), tests/test_preprocessing.py (new), tests/test_modelling.py (new), tests/test_file_io.py (new), tests/test_visualisation.py (new), tests/test_smoke.py (new), pyproject.toml (updated)
