# P001: Create config.py module
**Date:** 27-28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/config.py, src/lemmata/__init__.py
**Tests:** 120 passed

## Prompt Summary
Write src/lemmata/config.py with all constants: 5 languages (it, en, fr, de, es), slider ranges, TABLEAU10 colors, POS presets, DF thresholds, future-ready constants. Also create src/lemmata/__init__.py with APP_VERSION re-export. ~124 lines, 11 sections.

## Result
config.py created with 124 lines covering all configuration needs.

## Full Prompt

```
P001: Write src/lemmata/config.py --- the central configuration module.

Reference: ARCHITECTURE.md decisions 11-19, 25, 31, 52, 66, 121-122,
131-134.

Critical requirements:

-   SUPPORTED_LANGUAGES dict: it, en, de, fr, es → each with spaCy sm
    model name (e.g., "it_core_news_sm")

-   CHUNK_SIZE_MIN=300, CHUNK_SIZE_MAX=3000, CHUNK_SIZE_DEFAULT=1000

-   NUM_TOPICS_MIN=2, NUM_TOPICS_MAX=15, NUM_TOPICS_DEFAULT=5

-   WORDS_PER_TOPIC_MIN=5, WORDS_PER_TOPIC_MAX=30,
    WORDS_PER_TOPIC_DEFAULT=10

-   RANDOM_SEED=42

-   MAX_FILE_SIZE_MB=50, MAX_TOTAL_SIZE_MB=100

-   DEFAULT_POS_TAGS=["NOUN", "PROPN", "ADJ"]

-   POS_PRESETS dict: "Content words", "Content + verbs", "All open
    class", "Custom"

-   COLOR_PRIMARY="#0F6E56" (teal), COLOR_PALETTE="tableau10",
    COLOR_HEATMAP="viridis"

-   BG_MAIN="#F8F9FA", BG_SIDEBAR="#F0F2F6"

-   MIN_DF_AUTO and MAX_DF_AUTO thresholds by corpus size

-   Future-ready constants: NGRAM_RANGE=(1,1), MODEL_SIZE="sm",
    VECTORIZER_TYPE="count"

-   APP_VERSION="0.1.0"

-   All constants with type hints and docstrings

-   Create src/lemmata/\_\_init\_\_.py as well
```
