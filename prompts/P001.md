## P001 — Central configuration module
- **Date:** 2026-03-27
- **Category:** architecture
- **Prompt:** Write src/lemmata/config.py — the central configuration module. Reference: ARCHITECTURE.md decisions 11-19, 25, 31, 52, 66, 121-122, 131-134. Critical requirements: SUPPORTED_LANGUAGES, chunk/topic/POS constants, color/theme, DF auto-thresholds, future-ready constants, type hints, docstrings. Create __init__.py as well.
- **Response summary:** Created config.py with all constants organized by concern (app, languages, chunking, LDA, POS, files, DF thresholds, visual/theme, export, future-ready, text hygiene, logging). Created __init__.py re-exporting APP_VERSION. Added get_df_auto() helper for corpus-size-based min_df/max_df.
- **Decision:** DF_THRESHOLDS uses upper-bound dict pattern with 5 tiers (10/50/100/500/10000 docs). POS_PRESETS["Custom"] = empty list (UI fills). LDA_MAX_ITER=50 as sensible default (exposed in Advanced per decision 124).
- **Validation:** File creation verified.
- **Files changed:** src/lemmata/__init__.py (new), src/lemmata/config.py (new)
