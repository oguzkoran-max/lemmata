## P009 — Run test suite locally
- **Date:** 2026-03-28
- **Category:** testing
- **Prompt:** Run the test suite locally. Install dependencies, download spaCy model, run fast tests, then all tests. Fix any failures.
- **Response summary:** Installed lemmata with `pip install -e ".[test]"`, downloaded en_core_web_sm. First run: 116 passed, 1 failed (Altair theme API changed in v5.5+). Fixed visualisation.py to support both old `alt.themes` and new `alt.theme` API. Fixed test to check `alt.theme.names()` on new API. Also fixed pyproject.toml build-backend from invalid `setuptools.backends._legacy:_Backend` to `setuptools.build_meta`. Final run: **120 passed, 0 failed** in 8.62s.
- **Decision:** Altair theme registration now uses conditional: `alt.theme.register` decorator on >=5.5, `alt.themes.register` + `alt.themes.enable` on older versions. Build backend corrected to standard setuptools.
- **Validation:** `pytest -v` → 120 passed, 0 failed, 25 warnings (all deprecation, none from lemmata).
- **Files changed:** src/lemmata/visualisation.py (Altair theme compat fix), tests/test_visualisation.py (theme test fix), pyproject.toml (build-backend fix)
