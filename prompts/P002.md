## P002 — Text preprocessing pipeline
- **Date:** 2026-03-27
- **Category:** implementation
- **Prompt:** Write src/lemmata/preprocessing.py — the text preprocessing pipeline. Full pipeline: encoding detection, text hygiene, chunking, spaCy NLP, POS filtering, stopword handling, lemmatisation, language mismatch warning, preprocessing trace, minimum corpus check.
- **Response summary:** Created preprocessing.py with 6 public functions/entry points: detect_encoding(), clean_text(), chunk_text(), load_spacy_model(), process_documents(), plus internal _process_token(), _process_doc(), _strip_extension(). Pipeline: chardet detection → BOM/control/hyphen/multi-space cleanup → Unicode NFC → sentence-boundary chunking (single file) or separate docs (multi-file) → spaCy nlp.pipe() batch processing → POS filter → stopword removal (builtin + custom, counted separately) → lowercase lemmas. Returns (processed_texts, doc_labels, trace_dict) with full per-document token details for expander view.
- **Decision:** Used nlp.pipe() for batch processing efficiency. Per-token try-except wraps individual token processing (decision 105). Sentencizer added if no parser in pipeline. chardet confidence threshold set to 0.5. Custom stopwords lowercased on entry. Empty documents/chunks silently removed with count in trace.
- **Validation:** Not yet tested (no test suite).
- **Files changed:** src/lemmata/preprocessing.py (new)
