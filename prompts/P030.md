# P030: Implement coherence sweep for optimal topic selection
**Date:** 2 April 2026
**Commit:** "P030: Implement coherence sweep for optimal topic selection"
**Files changed:** src/lemmata/modelling.py, src/lemmata/visualisation.py, src/lemmata/app.py
**Tests:** 117 passed

## Prompt Summary
sweep_coherence() runs LDA for k=2 to 15, plots C_v vs k with best/current markers. Uses existing DTM, Gensim dictionary created once. Progress bar via callback. Results cached in session state.

## Result
Coherence sweep working (Zeno: best k=8, C_v 0.5279).

## Full Prompt

```
P030: Implement coherence sweep (optimal topic finder) ---
ARCHITECTURE.md decision 6

The sweep_coherence() stub already exists in modelling.py. Implement the
full feature:

1. IN src/lemmata/modelling.py --- fill in sweep_coherence():

- Takes: dtm (sparse matrix), feature_names (list[str]),
texts_for_coherence (list[list[str]]), k_range (range, default
2-16), random_state (int, default 42)

- For each k in k_range: fit LDA with k topics, compute C_v coherence

- Returns: dict with keys "k_values" (list[int]),
"coherence_scores" (list[float]), "best_k" (int --- k with highest
coherence)

- Use the SAME LDA parameters as run_lda() for consistency (max_iter
from config, etc.)

- This will be slow --- add a progress callback parameter:
progress_callback(current_k, total_k) so the UI can show progress

2. IN src/lemmata/visualisation.py --- add
create_coherence_sweep_chart():

- Takes: k_values (list[int]), coherence_scores (list[float]),
current_k (int, optional --- to highlight user's current selection)

- Returns: matplotlib Figure (NOT Altair --- we learned our lesson)

- Line chart: x = number of topics, y = coherence score (C_v)

- Mark the best_k point with a larger marker and annotation ("Best:
k=X")

- If current_k is provided, mark it with a different color marker
("Current: k=X")

- Teal color for the line (#0F6E56), clean style matching other
matplotlib charts

- Y-axis label: "Coherence (C_v)", X-axis label: "Number of topics"

- Integer x-axis ticks only

3. IN src/lemmata/app.py --- add sweep UI to the Overview tab:

- After the coherence banner and metrics, add an expander: "Find
optimal number of topics"

- Inside the expander:

- Brief explanation: "Run LDA with different topic counts (2-15) to
find the optimal number. Higher coherence generally indicates more
interpretable topics."

- A button: "Run Coherence Sweep" (teal colored)

- When clicked: run sweep_coherence() with st.progress() bar showing
progress

- After sweep completes: show the chart (st.pyplot) and a text
recommendation: "Optimal number of topics: X (C_v: Y.YYYY). You can
change the topic count in the sidebar and re-run analysis."

- Store sweep results in st.session_state so they persist across tab
switches

- The sweep should use the SAME preprocessed DTM that's already
computed --- do NOT re-preprocess

4. Add tests for sweep_coherence:

- Test that it returns correct keys (k_values, coherence_scores,
best_k)

- Test that best_k matches the k with highest coherence

- Test with small mock data (use existing test fixtures)

- Mark sweep test as @pytest.mark.slow (it runs multiple LDA fits)

Run fast tests: pytest -m "not slow" --tb=short -q

Commit as: "P030: Implement coherence sweep for optimal topic
selection"
```
