## P004 — File reading and export module
- **Date:** 2026-03-27
- **Category:** implementation
- **Prompt:** Write src/lemmata/file_io.py — file reading and export module. Support txt/pdf/odt/docx/epub reading, size validation, PDF-specific error messages, duplicate filename warning, file preview, text paste, ZIP export (topic_words.csv, doc_topic_matrix.csv, preprocessing_summary.csv, metrics.json, analysis_results.json, environment.json), PNG/SVG figure export, PDF report via reportlab, environment info collection.
- **Response summary:** Created file_io.py with 8 public functions: read_file(), read_files(), text_from_paste(), get_file_preview(), export_zip(), export_pdf_report(), export_figure_png(), export_figure_svg(), get_environment_info(), get_zip_filename(). Plus FileReadError exception class. Format readers: _read_txt (chardet via preprocessing.detect_encoding), _read_pdf (pdfplumber with 3 error types), _read_docx (python-docx), _read_odt (odfpy with recursive text extraction), _read_epub (ebooklib + HTML stripping). Export: ZIP with 6 files, PDF report via reportlab (cover + params + summary + topics + environment), PNG 300 DPI / SVG for matplotlib and Altair figures.
- **Decision:** Lazy imports for format-specific libraries (pdfplumber, python-docx, odfpy, ebooklib, reportlab) so missing optional deps give clear error messages rather than import-time crashes. _to_json helper handles numpy serialisation. Altair PNG export tries vl-convert first, falls back to chart.save(). Environment info uses importlib.metadata for package versions.
- **Validation:** Not yet tested (no test suite).
- **Files changed:** src/lemmata/file_io.py (new)
