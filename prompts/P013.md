# P013: Add GitHub Actions CI
**Date:** 29-30 March 2026
**Commit:** "P013: Add GitHub Actions CI workflow"
**Files changed:** .github/workflows/ci.yml
**Tests:** 117 passed

## Prompt Summary
Create .github/workflows/ci.yml: push/PR trigger, Python 3.11, pip install -e ".[test]", en_core_web_sm only, pytest -m "not slow".

## Result
CI green on first run (117 tests, 1m 8s). Note: file created via GitHub web UI due to OAuth workflow scope issue.

## Full Prompt

```
P013 Create .github/workflows/ci.yml for GitHub Actions CI.

Requirements:

- Trigger: push to main, pull_request to main

- Python 3.11 only (ARCHITECTURE.md decision 197)

- Steps:

1. Checkout repo

2. Set up Python 3.11

3. Install dependencies: pip install -e ".[dev]" (uses
pyproject.toml)

4. Install spaCy English model only (for smoke tests): python -m spacy
download en_core_web_sm

5. Run pytest excluding slow tests: pytest -m "not slow" --tb=short
-q

- Cache pip dependencies for speed

- Name the workflow "CI"

- Job name: "test"

Note: Do NOT install all 5 spaCy language models --- only en_core_web_sm
is needed for tests.

The smoke tests use @pytest.mark.slow so they will be excluded.

Regular tests use mock_nlp fixture, no real spaCy model needed.

Commit message: "P013: Add GitHub Actions CI workflow"
```
