# P004: Create file_io.py module
**Date:** 27-28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/file_io.py
**Tests:** 120 passed

## Prompt Summary
Write src/lemmata/file_io.py: read 5 formats (txt/pdf/docx/odt/epub), lazy imports, 3 PDF error paths. ZIP export (6 files), PDF report (reportlab), PNG 300DPI + SVG export. Environment info collector. ~470 lines.

## Result
file_io.py created with 10 public functions + 1 exception class.

## Full Prompt

```
P004: Write `src/lemmata/file_io.py` --- file reading and export
module.

Reference: ARCHITECTURE.md decisions 3, 28, 31, 40, 45-46, 56, 65, 67,
91, 119-120, 130, 162-163, 167.

Critical requirements:

1. FILE READING (decisions 3, 31, 162):

- Support: txt, pdf, odt, docx, epub

- File size validation: 50MB per file, 100MB total (from config)

- PDF errors: three specific messages --- protected,
scanned/image-only, corrupted (decision 162)

- Duplicate filename warning, no blocking (decision 163)

- Use detect_encoding() from preprocessing.py for txt files

- pdf: use pdfplumber or PyPDF2

- docx: use python-docx

- odt: use odfpy

- epub: use ebooklib

- Return list[dict] with {"filename": str, "content": str}

2. FILE PREVIEW (decision 65):

- Return metadata: file size, word count

- Return first 200 words for preview

3. TEXT PASTE (decision 91):

- Accept pasted text as alternative input

- Treat as single document with label "pasted_text"

4. EXPORT --- ZIP PACKAGE (decisions 45, 56, 119-120, 130):

- Main ZIP export containing:

- topic_words.csv

- doc_topic_matrix.csv

- preprocessing_summary.csv

- metrics.json

- analysis_results.json (machine-readable, decision 119)

- ZIP filename: lemmata\_{lang}\_{n}topics\_{date}\_{time}.zip
(decision 130)

- Standard CSV/JSON formats, no tool-specific formats (decision 120)

5. VISUAL EXPORT (decision 67):

- PNG export at 300 DPI

- SVG export

- Wordcloud PNG only (no SVG)

- Functions that take matplotlib/Altair figure objects and return bytes

6. PDF REPORT (decisions 9, 40):

- Generate PDF with reportlab

- Content: cover page + parameters + summary + topics + heatmap + text
excerpts + environment info

- Basic formatting, academic style

7. ENVIRONMENT REPORT (decision 46):

- Collect: Python version, package versions, all parameters, seed,
corpus info

- Include in ZIP and PDF report

Function signatures needed:

- read_file(file_bytes: bytes, filename: str) → dict

- read_files(uploaded_files: list) → tuple[list[dict], list[str]]
(returns texts, warnings)

- get_file_preview(file_bytes: bytes, filename: str) → dict

- export_zip(results: dict, params: dict) → bytes

- export_pdf_report(results: dict, params: dict) → bytes

- export_figure_png(fig, dpi: int = 300) → bytes

- export_figure_svg(fig) → bytes

- get_environment_info(params: dict) → dict

Import config values from config.py. Include type hints and docstrings.
```
