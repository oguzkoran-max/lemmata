# P014: Fix numpy serialization, heatmap export, distribution performance
**Date:** 30 March 2026
**Commit:** "P014: Fix numpy serialization, heatmap export, distribution performance"
**Files changed:** src/lemmata/file_io.py, src/lemmata/visualisation.py, src/lemmata/app.py
**Tests:** 117 passed

## Prompt Summary
Three fixes: (1) numpy JSON serialization with isinstance checks, (2) heatmap PNG/SVG with matplotlib fallback, (3) distribution tab top-50 limit for large corpora.

## Result
117 tests passed.

## Full Prompt

```
P014: Fix three known bugs in a single commit.

Bug 1 --- Numpy JSON serialization (file_io.py):

The \_default handler in the JSON export currently uses str(o) as
fallback for numpy types.

Replace with proper numpy-aware conversion:

- numpy integer types → int(o)

- numpy floating types → float(o)

- numpy ndarray → o.tolist()

- numpy bool\_ → bool(o)

- Keep str(o) as final fallback for unknown types

Bug 2 --- Heatmap PNG/SVG export (file_io.py + app.py):

vl-convert-python fails on Streamlit Cloud. Replace the Altair
chart.save() approach:

- For PNG: use chart.to_dict() → render with matplotlib (create heatmap
from the Altair spec data, save as PNG 300 DPI)

- For SVG: use chart.to_dict() → render with matplotlib (same approach,
save as SVG)

- Keep the try/except graceful fallback --- if matplotlib rendering
also fails, return None

- Update app.py heatmap tab to use the new export functions

Bug 3 --- Distribution tab performance (app.py):

When corpus has many chunks (e.g., 144), Altair renders slowly.

- Add max_rows limit: if n_docs > 50, show top-50 by dominant topic
weight + info message

- Use .properties(width='container') for responsive sizing

- Add @st.cache_data decorator to the distribution chart generation if
not already cached

Commit message: "P014: Fix numpy serialization, heatmap export,
distribution performance"
```
