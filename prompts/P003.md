# P003: Create modelling.py module
**Date:** 27-28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/modelling.py
**Tests:** 120 passed

## Prompt Summary
Write src/lemmata/modelling.py: CountVectorizer + scikit-learn LDA + Gensim C_v coherence. Topic reordering by descending avg weight. Convergence warning. DTM transparency dict. sweep_coherence() stub for future use. ~260 lines.

## Result
modelling.py created with 5 public functions + 1 helper.

## Full Prompt

```
P003: Write `src/lemmata/modelling.py` --- the LDA topic modeling
module.

Reference: ARCHITECTURE.md decisions 6, 25, 38, 97, 100, 118, 121-124.

Critical requirements:

1. VECTORIZATION (decision 97):

- CountVectorizer from scikit-learn

- min_df/max_df from get_df_auto() in config.py (decision 25)

- Import VECTORIZER_TYPE from config for future TF-IDF switch

2. LDA MODEL (decisions 121-124):

- scikit-learn LatentDirichletAllocation

- random_state from config.RANDOM_SEED (decision 121)

- max_iter from config.LDA_MAX_ITER

- Convergence warning if model used all iterations (decision 124)

- n_components from user parameter

3. TOPIC EXTRACTION:

- Extract top N words per topic with weights

- Order topics by corpus prevalence --- descending average weight
(decision 122)

- Topic labels: "Topic 1 (word1, word2, word3)" format (decision 33)

4. DOCUMENT-TOPIC MATRIX:

- Transform documents to topic distribution

- Assign dominant topic per document (decision 114)

5. COHERENCE SCORE (decision 18):

- C_v coherence using gensim CoherenceModel

- Also compute perplexity and log-likelihood from sklearn model

- Return all three metrics

6. DTM TRANSPARENCY (decision 103):

- Return vocabulary size, terms removed by min_df/max_df, final DTM
dimensions

7. FUTURE-READY (decisions 6, 100):

- sweep_coherence() stub for auto topic suggestion (decision 6)

- method parameter for future NMF/BERTopic (decision 100)

Function signatures needed:

- build_dtm(processed_texts: list[str], min_df, max_df) →
tuple[sparse_matrix, CountVectorizer, dict]

- run_lda(dtm, vectorizer, n_topics, random_seed, max_iter) →
tuple[model, doc_topic_matrix, list[dict], dict]

- compute_coherence(model, vectorizer, processed_texts, n_topics) →
dict

- get_topic_summary(model, vectorizer, n_words) → list[dict]

- sweep_coherence(...) → stub only

Import config values from config.py. Include type hints and docstrings.
```
