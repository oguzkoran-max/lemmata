# P011: Deploy to Streamlit Cloud
**Date:** 29 March 2026
**Commit:** 4 fix commits (reportlab pin, editable install, spaCy wheels, JSON/heatmap fallback)
**Files changed:** requirements.txt, pyproject.toml, src/lemmata/file_io.py, src/lemmata/visualisation.py
**Tests:** 120 passed

## Prompt Summary
Deploy to share.streamlit.io. Fix 4 deployment issues: (1) pin reportlab<5, (2) add -e . for editable install, (3) add spaCy model wheel URLs for 5 languages, (4) fix JSON serialization + heatmap export fallback.

## Result
App live at streamlit.app URL. 7 tabs working.

## Full Prompt

```
**P011: Streamlit Cloud Deploy**

---

**P011a: reportlab dependency fix**

Streamlit Cloud deploy error: poetry resolver cannot resolve reportlab
dependency.

1. In requirements.txt, change "reportlab>=4.0.0" to
"reportlab>=4.0.0,\<5"

2. In pyproject.toml, make the same change

3. git add requirements.txt pyproject.toml

4. git commit -m "P011: Pin reportlab\<5 for Streamlit Cloud
compatibility"

5. git push

---

**P011b: editable install for src layout**

Streamlit Cloud error: ModuleNotFoundError: No module named 'lemmata'.

The src layout package is not installed.

1. Add "-e ." as the LAST line in requirements.txt (this tells pip to
install the local package)

2. git add requirements.txt

3. git commit -m "P011: Add editable install to requirements.txt for
Streamlit Cloud"

4. git push

---

**P011c: spaCy model dependencies**

Streamlit Cloud error: spaCy model 'it_core_news_sm' not found.

Auto-download is failing on Streamlit Cloud.

Add spaCy model URLs to requirements.txt (before the "-e ." line):

https://github.com/explosion/spacy-models/releases/download/it_core_news_sm-3.8.0/it_core_news_sm-3.8.0-py3-none-any.whl

https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

https://github.com/explosion/spacy-models/releases/download/fr_core_news_sm-3.8.0/fr_core_news_sm-3.8.0-py3-none-any.whl

https://github.com/explosion/spacy-models/releases/download/de_core_news_sm-3.8.0/de_core_news_sm-3.8.0-py3-none-any.whl

https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

These are the 5 languages Lemmata supports (it, en, fr, de, es). Check
the correct version numbers --- they should match the installed spaCy
version. If 3.8.0 doesn't exist, find the latest sm model version.

git add, commit "P011: Add spaCy model dependencies for Streamlit
Cloud", push.

---

**P011d: Altair export fallback + JSON serialization**

---

P011 fix: Two remaining Streamlit Cloud errors.

Error 1 (Export tab): file_io.py line 708-710, \_to_json \_default
handler raises TypeError for non-serializable types. The \_default
function needs to handle numpy arrays (convert to list), numpy
integers/floats (convert to Python int/float), and any other
non-serializable objects.

Error 2 (Heatmap tab): The heatmap still crashes even with the previous
graceful fallback. Make sure \_tab_heatmap in app.py catches ALL
exceptions from export functions and renders the heatmap chart without
download buttons if export fails.

Fix both issues. Run tests. Single commit: "P011: Fix JSON
serialization + robust heatmap fallback", push.
```
