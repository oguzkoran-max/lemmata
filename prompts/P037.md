# P037: Fix tab font size with aggressive CSS targeting
**Date:** 2 April 2026
**Commit:** "P037: Fix tab font size — more aggressive CSS targeting"
**Files changed:** src/lemmata/app.py
**Tests:** 162 passed

## Prompt Summary
P036 CSS not taking effect. Applied !important with deep selectors targeting .stTabs [data-baseweb="tab-list"] button [data-testid="stMarkdownContainer"] p. Active tab: #0F6E56, font-weight 600. Highlight bar: 3px.

## Result
162 tests passed. Tab fonts now larger.

## Full Prompt

```
P037: Fix tab font size --- more aggressive CSS targeting

The tab styling from P036 is not taking effect. The tab labels

("Overview", "Topics", "Topic Map", etc.) are still small and
faint.

Try a more aggressive CSS approach with !important and multiple
selectors:

/* Target Streamlit tab buttons with high specificity */

.stTabs [data-baseweb="tab-list"] button
[data-testid="stMarkdownContainer"] p {

font-size: 1.1rem !important;

font-weight: 500 !important;

color: #555 !important;

}

/* Active tab */

.stTabs [data-baseweb="tab-list"] button[aria-selected="true"]
[data-testid="stMarkdownContainer"] p {

color: #0F6E56 !important;

font-weight: 600 !important;

}

/* Tab underline thicker */

.stTabs [data-baseweb="tab-highlight"] {

height: 3px !important;

background-color: #0F6E56 !important;

}

/* Also try direct button targeting */

.stTabs [data-baseweb="tab-list"] button {

font-size: 1.1rem !important;

}

If none of these CSS selectors work (Streamlit may have changed its

internal class names), try an alternative approach:

- Instead of CSS, replace st.tabs() with a custom tab bar using

st.columns() + st.button() styled buttons, OR

- Use st.radio(horizontal=True) as a tab selector with custom CSS

Test in browser: inspect the tab elements to find the actual CSS

class names being used, then target those specifically.

Also check: does the P036 CSS block exist in the code? Print the

first few lines of the custom CSS section to verify it was added.

Do not break existing 162 tests.

Run: pytest -m "not slow" --tb=short -q
```
