# P035: Add pedagogical guides, convergence warning, copyright/privacy notes
**Date:** 2 April 2026
**Commit:** "P035: Add pedagogical guides, convergence warning, copyright notice, and data processing note"
**Files changed:** src/lemmata/app.py
**Tests:** 162 passed

## Prompt Summary
(1) Topic interpretation guide expander in Topics tab. (2) Chunk size help text on slider. (3) Welcome screen already had "What is topic modeling?". (4) Convergence warning via model_info["convergence_warning"]. (5) Copyright notice already existed. (6) Privacy note in About expander.

## Result
162 tests passed.

## Full Prompt

```
P035: Add pedagogical guides, convergence warning, copyright notice, and
data processing note

Six UX/content improvements --- mostly text additions, minimal logic
changes:

1. TOPIC INTERPRETATION GUIDE (ARCHITECTURE.md decision 39):

In the Topics tab, add an expander "How to interpret topics" at the
top,

collapsed by default, containing:

- "Topics are statistical word clusters, not themes. LDA groups words
that

frequently co-occur across your documents."

- "Your role as a scholar is to interpret WHY these words appear
together.

Ask: What context connects these words? Which parts of the text drive

this topic? Does this cluster reflect a theme, a character, a setting,

or a stylistic pattern?"

- "Tip: Use the Representative Excerpt and Distribution tab to see
WHERE

each topic appears in your corpus."

- Keep it concise: 4-5 short paragraphs max.

2. CHUNK SIZE GUIDE (ARCHITECTURE.md decision 11):

In the sidebar, add help text to the chunk size slider (use the help=

parameter or a small st.caption below):

- "Smaller chunks (300-500) → more documents, finer-grained analysis,

but noisier topics. Larger chunks (1500-3000) → fewer documents, more

stable topics, but less detail. Default (1000) works well for most
texts."

3. WELCOME SCREEN ENHANCEMENT (ARCHITECTURE.md decision 86):

On the welcome screen (before any files are uploaded), check if there is

already a "What is topic modeling?" section. If not, add a brief
expander:

- "What is topic modeling?" --- 2-3 sentences explaining LDA in plain
language

- "What can Lemmata do?" --- short bullet list: upload texts,
automatic

preprocessing, discover topics, explore results, export for publication

- Keep it inviting and non-technical.

4. CONVERGENCE WARNING (ARCHITECTURE.md decision 124):

After LDA fitting, check if the model used all max_iter iterations
without

converging. In scikit-learn LDA, check the n_iter\_ attribute:

- If model.n_iter\_ == max_iter, the model may not have converged

- Show st.warning in Overview tab: "The model used all {max_iter}
iterations

without fully converging. Consider increasing max iterations in Advanced

settings, or reducing the number of topics."

- Store this as a flag in results dict (e.g.,
results["convergence_warning"])

5. COPYRIGHT NOTICE (ARCHITECTURE.md decision 173):

Below the file upload area, add a small st.caption:

- "You are responsible for ensuring you have the right to upload and

analyze these texts."

- Use st.caption (small gray text), not st.info or st.warning.

6. DATA PROCESSING NOTE (ARCHITECTURE.md decision 178):

In the sidebar About expander, add:

- "Privacy: Texts are processed in memory during your session. No data
is

stored permanently. No data is shared with third parties."

- If there is already an About expander, append to it. If not, create
one.

Implementation notes:

- These are mostly text/UI additions with minimal logic (only #4 has
real logic)

- For #4, the convergence check should be in modelling.py, result
passed to app.py

- Add tests only for #4 (convergence detection logic)

- Do not break existing 162 tests

- Run: pytest -m "not slow" --tb=short -q
```
