# P020: Fix distribution and diachronic chart rendering (failed attempt)
**Date:** 1-2 April 2026
**Commit:** "P020: Fix distribution and diachronic chart rendering on Streamlit Cloud"
**Files changed:** src/lemmata/visualisation.py
**Tests:** 117 passed

## Prompt Summary
Attempted to fix Altair chart rendering by modifying mark_bar() and legend parameters. Charts still blank on Streamlit Cloud.

## Result
Fix did not work. Root cause: Streamlit Cloud Vega-Lite renderer issue, not Altair encoding.

## Full Prompt

```
P020: Fix distribution and diachronic chart rendering on Streamlit Cloud

I have two Altair chart rendering bugs on Streamlit Cloud. The charts
work in the code but render as invisible/blank in the browser.

First, check the Altair version:

cat requirements.txt \| grep altair

python -c "import altair; print(altair.\_\_version\_\_)"

Then fix both issues in src/lemmata/visualisation.py:

1. DISTRIBUTION CHART (create_distribution_chart, line 315):

The stacked bars are invisible. Replace the current chart with this
approach:

- Keep the same DataFrame structure

- Use alt.Chart(df).mark_bar() WITHOUT the opacity parameter

- Add opacity as an encoding: alt.Opacity(value=0.85) or just remove
opacity entirely

- Keep everything else the same (stack="normalize", color, tooltip)

2. DIACHRONIC CHART (create_diachronic_chart, line 381):

The lines are invisible. Replace the current encoding approach:

- Remove detail="Topic:N" from encode()

- Instead, add color=alt.Color("Topic:N", scale=color_scale,
legend=alt.Legend(title="Topic"))

- The color encoding on a nominal field should automatically group the
lines

- Keep mark_line(strokeWidth=2) and all other settings

Run the tests after changes: pytest -m "not slow" --tb=short -q

Commit as: "P020: Fix distribution and diachronic chart rendering on
Streamlit Cloud"
```
