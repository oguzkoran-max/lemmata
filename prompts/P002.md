# P002: Create preprocessing.py module
**Date:** 27-28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/preprocessing.py
**Tests:** 120 passed

## Prompt Summary
Write src/lemmata/preprocessing.py with 5 public functions: detect_encoding (chardet, 0.5 threshold), clean_text (hygiene pipeline), chunk_text (word count + sentence boundary), spaCy processing (nlp.pipe batch), POS filter + stopwords + lemmatization. Per-token try-except. ~300 lines.

## Result
preprocessing.py created with full pipeline.

## Full Prompt

```
P002: Write `src/lemmata/preprocessing.py` --- the text preprocessing
pipeline.

Reference: ARCHITECTURE.md decisions 4, 12, 14, 32, 37, 52-54, 58-59,
63-64, 66, 105, 112-113, 126, 165-167, 170.

This is the most complex module. It handles everything between raw file
content and the document-term matrix.

Critical requirements:

1. ENCODING DETECTION (decision 167):

- Auto-detect with chardet → UTF-8 → Latin-1 fallback chain

- Return detected encoding in preprocessing trace

2. TEXT HYGIENE (decision 170, 37):

- Remove BOM, null bytes, control characters

- Unicode NFC normalization

- Line-end hyphen joining (e.g., "pre-\\nprocessing" →
"preprocessing")

- Multi-space collapse

- Normalize line endings to \\n

3. CHUNKING (decisions 4, 32, 113):

- If single file uploaded → chunk by word count target

- Respect sentence boundaries using spaCy sentence detection

- No overlap between chunks

- Chunk labels: [filename]\_001 format (decision 42)

- Skip empty chunks silently, note in preprocessing summary (decision
166)

4. spaCy PROCESSING (decisions 52, 105, 112):

- Load sm model for selected language (@st.cache_resource pattern
ready)

- Per-token try-except for error tolerance (decision 105)

- Italian contractions handled correctly by spaCy default (decision
112)

- Full UTF-8 support, non-Latin scripts tokenized but not lemmatized
(decision 165)

5. POS FILTERING (decision 12):

- Filter by user-selected POS tags

- Default: NOUN, PROPN, ADJ

- Use POS_PRESETS from config.py

6. STOPWORD HANDLING (decisions 14, 59):

- spaCy built-in stopwords + user custom stopwords

- Track counts: "Stopwords removed: N (built-in: X, custom: Y)"

7. LEMMATIZATION (decision 126):

- spaCy default lemmatizer

- Lowercase all lemmas

8. LANGUAGE MISMATCH WARNING (decision 58):

- If token ratio \< 10% after processing → return warning flag

9. PREPROCESSING TRACE (decision 17):

- Return summary dict: original_tokens, after_stopwords, after_pos,
final_lemmas, encoding_used, chunks_created, empty_chunks_removed,
stopwords_removed_builtin, stopwords_removed_custom

- Detailed token table data for expander view

10. MINIMUM CORPUS CHECK (decision 106):

- Warning if \< 50 unique lemmas, no blocking

Function signatures needed:

- detect_encoding(raw_bytes: bytes) → str

- clean_text(text: str) → str

- chunk_text(text: str, filename: str, chunk_size: int, nlp) →
list[dict]

- process_documents(texts: list[dict], language: str, pos_tags:
list[str], custom_stopwords: set[str], chunk_size: int) →
tuple[list[str], list[str], dict]

(returns: processed_texts, doc_labels, preprocessing_trace)

Import config values from config.py. Include type hints and docstrings
for all functions.
```
