## P005 — Visualisation module
- **Date:** 2026-03-27
- **Category:** implementation
- **Prompt:** Write src/lemmata/visualisation.py — all charts and visual components. Topic bars (Altair), wordcloud (matplotlib), heatmap (Altair viridis), distribution (stacked bar), diachronic (line + file boundaries), topic map (pyLDAvis + scatter fallback), top lemmas, coherence display, colour consistency (tableau10), Altair theme.
- **Response summary:** Created visualisation.py with 10 public functions + 2 helpers. Zero st.* calls — returns figures/data only. All Altair charts use registered "lemmata" theme (white bg, minimal axes, light grid, sans-serif). Fixed colour per topic via explicit _TABLEAU10 hex list. pyLDAvis with try-except → PCA scatter fallback. Wordcloud via matplotlib with topic colour. Coherence display: green/yellow/red bands with text suggestions, no emoji.
- **Decision:** _TABLEAU10 stored as explicit hex list (not relying on Altair's internal scheme accessor) to guarantee same colour across matplotlib wordcloud and Altair charts. Dominant topic in distribution chart uses opacity=1.0 vs 0.5 for non-dominant. Diachronic chart file boundaries rendered as dashed grey vertical rules. Topic map PCA fallback uses circle size for prevalence.
- **Validation:** Not yet tested (no test suite).
- **Files changed:** src/lemmata/visualisation.py (new)
