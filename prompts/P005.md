# P005: Create visualisation.py module
**Date:** 27-28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/visualisation.py
**Tests:** 120 passed

## Prompt Summary
Write src/lemmata/visualisation.py: Altair charts (topic bars, heatmap, distribution, diachronic, top lemmas, topic map fallback), matplotlib wordcloud, pyLDAvis HTML or PCA scatter fallback. TABLEAU10 explicit hex. configure_altair_theme(). Zero st.* calls. ~480 lines.

## Result
visualisation.py created with 10 public functions.

## Full Prompt

```
P005: Write `src/lemmata/visualisation.py` --- all charts and visual
components.

Reference: ARCHITECTURE.md decisions 15-16, 34, 41, 51, 61, 66-67, 92,
111, 114-115, 125, 144-150.

Critical requirements:

1. TOPIC BAR CHARTS (decisions 15, 51, 144):

- Altair interactive bar chart (hover tooltips) as default view

- Show word + weight per topic

- White background, minimal axes, light grid, sans-serif

- Width 600px

- Corpus frequency data available in expander view

2. WORDCLOUD (decisions 15, 111, 148):

- matplotlib wordcloud as toggle alternative

- Rectangle, white background, 800x400px

- Topic name above, nothing below

- PNG export only (no SVG for wordcloud)

3. TOPIC MAP / pyLDAvis (decision 16):

- Optional pyLDAvis integration with try-except

- If pyLDAvis fails → 2D scatter fallback (Altair)

- Lazy loading (decision 69)

4. HEATMAP (decisions 145, 150):

- Altair heatmap, viridis colormap

- Hover tooltip for values, no numbers in cells

- Dynamic sizing: height=max(200, n_docs*25), width=max(300,
n_topics*60)

5. DISTRIBUTION CHART (decision 114):

- Document-topic distribution visualization

- Dominant topic highlighted per document

6. DIACHRONIC VIEW (decision 115):

- Topic weight trend line chart

- X=document order, Y=weight

- Vertical lines at file boundaries

7. TOP LEMMAS BAR CHART (decision 92):

- Pre-LDA: top 20 frequent lemmas bar chart for Overview tab

8. COHERENCE DISPLAY (decision 41):

- Color-coded background: green (>0.5) / yellow (0.3-0.5) / red
(\<0.3)

- Text explanation + action suggestion

- No emoji

9. COLOR CONSISTENCY (decisions 61, 66, 125):

- Fixed color per topic using tableau10 (consistent across all tabs)

- Color-blind-friendly

- Import COLOR_PALETTE, COLOR_HEATMAP from config.py

10. THEME HELPER:

- Function to generate consistent Altair theme config

- Apply config.py colors throughout

Function signatures needed:

- create_topic_bars(topic_words: list[dict], topic_id: int, color:
str) → alt.Chart

- create_wordcloud(topic_words: dict, width: int = 800, height: int =
400) → Figure

- create_heatmap(doc_topic_matrix, doc_labels, topic_labels) →
alt.Chart

- create_distribution_chart(doc_topic_matrix, doc_labels, topic_labels)
→ alt.Chart

- create_diachronic_chart(doc_topic_matrix, doc_labels, topic_labels,
file_boundaries) → alt.Chart

- create_topic_map(model, dtm, vectorizer) → HTML or alt.Chart

- create_top_lemmas_chart(lemma_counts: dict, n: int = 20) → alt.Chart

- get_coherence_display(c_v: float) → dict

- get_topic_color(topic_id: int) → str

- configure_altair_theme() → None

Import config values from config.py. Include type hints and docstrings.
```
