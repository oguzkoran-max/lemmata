# P032: Add topic-document safeguard and imbalanced corpus warning
**Date:** 2 April 2026
**Commit:** "P032: Add topic-document safeguard and imbalanced corpus warning"
**Files changed:** src/lemmata/app.py, src/lemmata/preprocessing.py, tests/
**Tests:** 147 passed (+15 new)

## Prompt Summary
(1) estimate_n_documents() + calc_topic_max() dynamically cap topic slider at max(2, n_docs//2) with warning. (2) check_imbalanced_corpus() warns if largest file >10x smallest.

## Result
147 tests passed (+15 new).

## Full Prompt

```
P032: Add topic-document safeguard and imbalanced corpus warning
(decisions 128, 164)

Two small but important UX protections:

1. TOPIC > DOCUMENT SAFEGUARD (decision 164) in src/lemmata/app.py:

- After files are uploaded and chunks are created, calculate the number
of documents/chunks

- Dynamically set the topic slider maximum to max(2, n_documents // 2)

- If user's current topic count exceeds n_documents // 2, show
st.warning: "You have N documents. Using more than N/2 topics may
produce unreliable results. Consider reducing the topic count."

- Do NOT block analysis --- just warn

2. IMBALANCED CORPUS WARNING (decision 128) in src/lemmata/app.py:

- After files are uploaded (but before analysis), check word counts
across uploaded files

- If the largest file is more than 10x the size of the smallest file,
show st.info: "Large size difference detected between files (largest is
Xx bigger than smallest). This may cause topics to be dominated by the
larger file. Consider using similar-length texts or adjusting chunk
size."

- Only show this when multiple files are uploaded (not for single
file + chunking)

- Do NOT block analysis --- just inform

3. Tests:

- Test that topic safeguard calculates correct max (e.g., 10 docs → max
5 topics)

- Test that imbalanced warning triggers at 10x ratio and not at 5x

Run tests: pytest -m "not slow" --tb=short -q

Commit as: "P032: Add topic-document safeguard and imbalanced corpus
warning"

---

P032: Add topic-document safeguard and imbalanced corpus warning

Two UX improvements for edge cases:

1. TOPIC-DOCUMENT SAFEGUARD (ARCHITECTURE.md decision 164):

In app.py sidebar, dynamically cap the "Number of topics" slider
maximum

at max(2, num_documents // 2) once the document count is known (after

file upload or text paste, before analysis runs).

- If uploaded files produce fewer documents than the current slider
value

allows, reset the slider to the new max.

- Show st.warning if selected topics > document_count // 2:

"You have {n} documents. More than {n//2} topics may produce unreliable

results."

- The slider minimum stays at 2 (already in config.py).

- This applies AFTER chunking --- use the actual chunk/document count,

not the number of uploaded files.

2. IMBALANCED CORPUS WARNING (ARCHITECTURE.md decision 128):

After files are uploaded (multi-file upload only, 2+ files), calculate:

ratio = max_file_size / min_file_size (in characters or bytes)

If ratio > 10, show st.info:

"Your largest file is {ratio:.0f}x bigger than your smallest. This

imbalance may cause larger files to dominate topic distributions.

Consider splitting large files into chapters or sections."

- Only for multi-file uploads (skip for single file or pasted text)

- Show once, before analysis runs

- Non-blocking (st.info, not st.error)

Implementation notes:

- Keep all warnings non-blocking (info/warning, not error)

- Add tests for both features (test the warning logic, not Streamlit
UI)

- Do not break existing 132 tests

- Run: pytest -m "not slow" --tb=short -q
```
