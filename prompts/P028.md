# P028: Add scroll fade-in animations to landing page
**Date:** 2 April 2026
**Commit:** "P028: Add scroll fade-in animations to landing page"
**Files changed:** docs/index.html
**Tests:** 117 passed

## Prompt Summary
CSS .fade-in class (opacity 0 to 1, translateY 30 to 0, 0.6s ease-out) + IntersectionObserver JS (threshold 0.1). Applied to 16 elements. Hero and nav excluded.

## Result
Smooth scroll animations on landing page.

## Full Prompt

```
P028: Add subtle scroll fade-in animations to landing page

In docs/index.html, add CSS-only fade-in animations for sections as the
user scrolls down. Use Intersection Observer API (minimal JavaScript, no
external libraries).

1. Add this CSS inside the existing \<style> block:

.fade-in {

opacity: 0;

transform: translateY(30px);

transition: opacity 0.6s ease-out, transform 0.6s ease-out;

}

.fade-in.visible {

opacity: 1;

transform: translateY(0);

}

2. Add this JavaScript just before \</body>:

\<script>

const observer = new IntersectionObserver((entries) => {

entries.forEach(entry => {

if (entry.isIntersecting) {

entry.target.classList.add('visible');

}

});

}, { threshold: 0.1 });

document.querySelectorAll('.fade-in').forEach(el =>
observer.observe(el));

\</script>

3. Add the class "fade-in" to these elements (find them by their
content/structure):

- Each feature card (the 5 cards in the Features section)

- Each "How it Works" step (the 3 numbered circles with text)

- The "Powered By" section content

- The "Cite" section content

- The final CTA section

Do NOT add fade-in to the hero section or nav --- those should be
visible immediately on page load.

Test: open docs/index.html locally to verify animations work.

Commit as: "P028: Add scroll fade-in animations to landing page"
```
