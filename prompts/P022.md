# P022: Fix language mismatch warning using stop-word detection
**Date:** 2 April 2026
**Commit:** "P022-debug: Add temporary debug output..." + "P022: Fix language mismatch warning using stop-word detection"
**Files changed:** src/lemmata/preprocessing.py, src/lemmata/app.py
**Tests:** 117 passed

## Prompt Summary
Two bugs found and fixed: (1) spaCy sm models have empty word vectors, making is_oov always True -- switched to is_stop-based detection, (2) st.rerun() was clearing the warning -- fixed display timing.

## Result
Language warning works correctly for all 4 tested scenarios.

## Full Prompt

```
P022: Move language mismatch warning before main preprocessing pipeline

Currently the language_warning check happens too late --- the pipeline
crashes before reaching it. The fix: check language compatibility EARLY,
right after spaCy model loads but BEFORE the full preprocessing runs.

In src/lemmata/preprocessing.py, find where the language_warning key is
set. Move the language detection logic to happen earlier:

1. After spaCy model is loaded and before full text processing begins,
take a SAMPLE of the input text (first 500 characters or first 5
sentences, whichever is shorter)

2. Run spaCy on this sample

3. Count how many tokens are recognized (token.is_oov == False) vs
total tokens (excluding punctuation and spaces)

4. If recognition rate is below 10%, set language_warning in the
results dict AND show it immediately --- but do NOT stop the pipeline.
Let it continue and fail naturally if it will.

In src/lemmata/app.py, check for language_warning BEFORE checking for
errors:

- After analysis completes (or fails), first check if results dict has
language_warning key

- If yes, show st.warning() with the language mismatch message

- THEN show the error message if analysis failed

This way the user sees BOTH: the yellow warning ("you might have the
wrong language selected") AND the red error ("analysis failed"),
which gives them a clear diagnosis.

Run tests: pytest -m "not slow" --tb=short -q

Commit as: "P022: Move language mismatch check before pipeline to catch
early failures"

P022-debug:

---

P022-debug: The st.warning is still not showing. I suspect there's an
OUTER try/except wrapping \_run_analysis that catches the exception
before the inner handler runs, OR st.rerun() is clearing the warning.

Show me the EXACT code that CALLS \_run_analysis. Print the 30 lines
around where \_run_analysis is called:

cat src/lemmata/app.py \| sed -n '550,600p'

Also add a temporary debug line. In the \_run_analysis function, inside
the except block, RIGHT BEFORE the st.warning line, add:

st.info(f"DEBUG: early_lang_warning = {early_lang_warning}")

Push this debug version so we can see what's happening on Streamlit
Cloud.

Do NOT commit as a permanent change --- just push so we can test. Commit
as: "P022-debug: Add temporary debug output for language warning"

P022-final:

---

P022-final: Remove debug line and finalize language warning fix

1. Remove the st.info(f"DEBUG: early_lang_warning = ...") line from
app.py --- it was only for testing

2. Keep everything else exactly as is --- the language warning is
working correctly now

3. Run tests: pytest -m "not slow" --tb=short -q

Commit as: "P022: Fix language mismatch warning using stop-word
detection"
```
