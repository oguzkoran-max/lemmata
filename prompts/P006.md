## P006 — Streamlit application (app.py)
- **Date:** 2026-03-27
- **Category:** implementation
- **Prompt:** Write src/lemmata/app.py — the main Streamlit application. UI layer wiring all five modules. Page config, sidebar (logo, language, params, POS, stopwords, advanced, about, cite, footer), file upload + paste, welcome screen, 7 result tabs (Overview, Topics, Topic Map, Heatmap, Distribution, Preprocessing, Export), analysis flow with st.status, caching, session state, error handling.
- **Response summary:** Created app.py (~580 lines) as pure UI layer. Sidebar: λ logo + params + POS presets + custom stopwords + advanced expander + about/cite/feedback/footer. Main area: file upload with preview + text paste + welcome screen. Analysis: st.status 6-step progress, @st.cache_resource for spaCy, session_state for results. 7 tabs: Overview (coherence display + DTM info + top lemmas), Topics (selector + bar/wordcloud toggle + table + excerpt), Topic Map (pyLDAvis/fallback), Heatmap (with PNG/SVG download), Distribution (stacked bar + diachronic), Preprocessing (summary + per-doc + token-level detail), Export (ZIP + PDF + individual CSVs/JSON). Error handling: try-except with st.error + traceback expander.
- **Decision:** Run Analysis button placed in sidebar after _render_sidebar() returns params so it can check has_files. Re-run clears previous results with info message. n_topics clamped to doc_count/2 (decision 164). File boundaries computed by matching doc_labels to source filenames. Used st.rerun() after successful analysis to refresh all tabs with new session state. Altair charts exported via export_figure_png/svg from file_io (handles both Altair and matplotlib).
- **Validation:** Not yet tested (dependencies not installed).
- **Files changed:** src/lemmata/app.py (new)
