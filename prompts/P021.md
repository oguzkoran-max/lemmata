# P021: Replace charts with matplotlib for Streamlit Cloud compatibility
**Date:** 2 April 2026
**Commit:** "P021: Replace distribution and diachronic charts with matplotlib for Streamlit Cloud compatibility"
**Files changed:** src/lemmata/visualisation.py, src/lemmata/app.py, tests/test_visualisation.py
**Tests:** 117 passed

## Prompt Summary
Replace Altair with matplotlib for distribution (barh) and diachronic (line plot) charts. TABLEAU10 colors. st.altair_chart() to st.pyplot(). 3 tests updated.

## Result
Both charts now render correctly on Streamlit Cloud.

## Full Prompt

```
P021: Replace distribution and diachronic charts with matplotlib (Altair
rendering broken on Streamlit Cloud)

Both the distribution stacked bar chart and the diachronic line chart
render as blank/invisible on Streamlit Cloud despite correct Altair
code. This is a Vega-Lite renderer issue. Replace both with matplotlib
implementations.

IMPORTANT: Do NOT touch any other charts (topic bars, heatmap, top
lemmas, topic map). Only replace these two specific functions.

1. DISTRIBUTION CHART --- replace create_distribution_chart in
src/lemmata/visualisation.py:

- Replace the Altair implementation with matplotlib

- Create a horizontal stacked bar chart (barh)

- Use the TABLEAU10 colors already defined in the file

- X-axis: topic proportion (0% to 100%, normalized)

- Y-axis: document labels

- Legend showing topic labels

- Return a matplotlib Figure object instead of alt.Chart

- Keep the same function signature (doc_topic_matrix, doc_labels,
topic_labels)

- But change return type from alt.Chart to matplotlib.figure.Figure

2. DIACHRONIC CHART --- replace create_diachronic_chart in
src/lemmata/visualisation.py:

- Replace the Altair implementation with matplotlib

- Line chart: x = document order (0 to n), y = topic weight (0 to 1)

- One line per topic, colored with TABLEAU10

- Optional dashed vertical lines at file_boundaries

- Legend showing topic labels

- Return a matplotlib Figure object

- Keep the same function signature but change return type

3. UPDATE app.py \_tab_distribution function:

- The distribution chart and diachronic chart currently use
st.altair_chart()

- Change both to st.pyplot(fig) since they now return matplotlib
Figures

- Remove use_container_width=True from those two calls (st.pyplot
doesn't use it the same way)

- Keep everything else in \_tab_distribution exactly the same

4. UPDATE any tests that check return types for these two functions ---
they should expect matplotlib Figure instead of alt.Chart.

Run tests: pytest -m "not slow" --tb=short -q

Commit as: "P021: Replace distribution and diachronic charts with
matplotlib for Streamlit Cloud compatibility"
```
