# P036: Improve UI visibility -- tab styling, headers, sidebar summary
**Date:** 2 April 2026
**Commit:** "P036: Improve UI visibility — tab styling, section headers, and minor fixes"
**Files changed:** src/lemmata/app.py
**Tests:** 162 passed

## Prompt Summary
(1) Tab CSS: font-size 1.05rem, inactive #555, active border 3px. (2) Remove duplicate Top Lemmas chart title. (3) Sidebar summary in st.container(border=True). (4) "What to do next?" guide in Overview. (5) Estimated time moved above Run button.

## Result
162 tests passed.

## Full Prompt

```
P036: Improve UI visibility --- tab styling, section headers, and minor
fixes

Visual/UX improvements based on user testing:

1. TAB STYLING --- MORE PROMINENT:

Using st.markdown with custom CSS, make the tab bar more visible:

- Increase tab font size (try font-size: 1.05rem or 1.1rem)

- Make inactive tabs darker (not light gray, use a medium gray like
#555)

- Make the active tab indicator (teal underline) thicker (3px instead
of 2px)

- Add slight bottom border to the entire tab bar for visual separation

Target selector: div[data-baseweb="tab-list"] button

Keep changes minimal and test that they don't break on mobile.

2. DUPLICATE "Top Lemmas" HEADING:

In the Overview tab, "Top Lemmas (pre-LDA)" appears twice --- once as
a

Streamlit section header (st.subheader or st.markdown) and once as the

matplotlib chart title. Remove the chart title (set title="" in the

matplotlib figure) since the section header already provides the label.

3. SIDEBAR POST-ANALYSIS SUMMARY ENHANCEMENT:

The current "5 topics · C_v: 0.52 · 5911 lemmas" text is good but very

small and easy to miss. Wrap it in a st.container(border=True) or use

st.success/st.info to make it more visible. Something like a small

colored box that stands out from the gray sidebar.

4. "WHAT'S NEXT?" GUIDE IN OVERVIEW:

After the metrics and Top Lemmas chart in Overview, add a collapsed

expander "What to do next?" with brief guidance:

- "Review Topics: Go to the Topics tab to examine each topic's words

and representative text excerpts."

- "Check Distribution: See which topics dominate which parts of your

corpus in the Distribution tab."

- "Explore the Map: The Topic Map shows how topics relate to each
other."

- "Export: Download your results as CSV, PDF, or a complete ZIP
archive

from the Export tab."

5. ESTIMATED TIME POSITION:

Currently "Estimated analysis time: ~45s" is at the very bottom of
the

sidebar, below the version info. Move it to just above or just below the

"Run Analysis" button where it's more relevant and visible.

Implementation notes:

- CSS changes via st.markdown(unsafe_allow_html=True) --- keep minimal

- Do not break existing 162 tests

- Run: pytest -m "not slow" --tb=short -q
```
