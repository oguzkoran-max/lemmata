# P009: Run tests and fix issues
**Date:** 28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** src/lemmata/visualisation.py, tests/test_visualisation.py, pyproject.toml
**Tests:** 120 passed

## Prompt Summary
Run pytest, fix 3 issues: (1) pyproject.toml build-backend to setuptools.build_meta, (2) Altair theme registration old+new API compat, (3) test_visualisation theme check updated.

## Result
120 tests passed, 0 failed, 8.62s. Determinism confirmed.

## Full Prompt

```
P009: Run the test suite locally.

Steps:

1. Install dependencies: pip install -e ".[dev]" or pip install -r
requirements.txt

2. Install pytest if not in requirements: pip install pytest

3. Download spaCy English model: python -m spacy download
en_core_web_sm

4. Run fast tests first: pytest -m "not slow" -v

5. If fast tests pass, run all: pytest -v

6. Report: which tests pass, which fail, and fix any failures.

Do NOT skip failing tests --- fix the code until all tests pass.
```
