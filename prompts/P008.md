# P008: Write test suite
**Date:** 28 March 2026
**Commit:** "P001-P009: Initial commit — all 6 modules, tests, infrastructure"
**Files changed:** tests/conftest.py, tests/test_config.py, tests/test_preprocessing.py, tests/test_modelling.py, tests/test_file_io.py, tests/test_visualisation.py, tests/test_smoke.py
**Tests:** 120 passed

## Prompt Summary
Write 8 test files: conftest.py (fixtures), test_config.py (27), test_preprocessing.py (16), test_modelling.py (15, incl. determinism), test_file_io.py (22), test_visualisation.py (17), test_smoke.py (3, @pytest.mark.slow). ~120 tests total.

## Result
120 tests created across 8 files.

## Full Prompt

```
P008: Write tests in `tests/` directory.

Reference: ARCHITECTURE.md decisions 50, 78, 182.

Critical requirements:

1. `tests/\_\_init\_\_.py` --- empty

2. `tests/conftest.py`:

- Shared fixtures: sample Italian text, sample English text, sample
corpus (3+ short documents)

- Fixture for config defaults

3. `tests/test_config.py`:

- All constants exist and have correct types

- get_df_auto() returns valid (min_df, max_df) tuples for various
corpus sizes

- POS_PRESETS has all 4 presets

- SUPPORTED_LANGUAGES has 5 languages with valid spaCy model name
format

4. `tests/test_preprocessing.py`:

- detect_encoding() returns valid encoding string

- clean_text() removes BOM, null bytes, control chars, joins hyphenated
words, collapses spaces

- chunk_text() respects sentence boundaries, produces correct label
format [filename]\_001

- chunk_text() skips empty chunks

- process_documents() returns (processed_texts, doc_labels, trace) with
correct types

- Trace dict contains all required keys

5. `tests/test_modelling.py`:

- build_dtm() returns sparse matrix + vectorizer + dtm_info dict

- run_lda() returns model + doc_topic_matrix + topic_summaries +
model_info

- DETERMINISM TEST (decision 50, CRITICAL): run LDA twice with same
seed → identical results

- Topic ordering is by descending average weight

- compute_coherence() returns dict with "c_v" key

- sweep_coherence() raises NotImplementedError

6. `tests/test_file_io.py`:

- read_file() handles txt bytes correctly

- get_file_preview() returns size, word_count, preview text

- text_from_paste() returns correct dict format

- export_zip() returns valid ZIP bytes

- get_environment_info() returns dict with required keys

7. `tests/test_visualisation.py`:

- create_topic_bars() returns alt.Chart

- create_wordcloud() returns matplotlib Figure

- create_heatmap() returns alt.Chart

- get_coherence_display() returns correct color for different c_v
ranges

- get_topic_color() returns valid hex string

- configure_altair_theme() runs without error

8. `tests/test_smoke.py` (decision 182):

- Integration test: upload sample text → run full pipeline → verify
outputs exist

- End-to-end: text → preprocess → build_dtm → run_lda →
compute_coherence → export_zip

- Verify ZIP contains all 6 expected files

Use pytest. Mark slow tests with @pytest.mark.slow. Keep fixtures
minimal --- no spaCy model download in unit tests (mock where needed),
but smoke test can use real model.
```
